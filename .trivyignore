# .trivyignore — Suppress false positives and unfixable base image CVEs
# Format: CVE-ID [expiry-date] [comment]

# openssl CVE in python:3.12-slim base image
# Fixed version 3.5.6-1~deb13u2 not yet available in debian:trixie apt repos
# Remove this entry once the base image is updated
CVE-2026-45447
